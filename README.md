# anony-chatroom

簡易匿名聊天室（Node.js + SSE）。

## 功能
- 匿名聊天與在線名單。
- 訊息時間標記（以每位使用者裝置本地時區顯示 hh:mm）。
- 可選密語加密（前端簡易 XOR）。
- Emoji 與 GIF 貼圖（支援關鍵字篩選與刷新隨機結果）。
- 網路監控可見性提示（根據 HTTPS 與密語狀態即時顯示風險）。

## 啟動
```bash
npm start
```

預設埠號：`8080`（可透過 `PORT` 環境變數覆蓋）。
